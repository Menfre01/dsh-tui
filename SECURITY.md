# Security Policy

## 报告漏洞

如果你发现安全漏洞，请**不要**提交公开 Issue。

请发送邮件至 menfre@proton.me，以 PGP 加密为佳。

我们会在 48 小时内确认收到报告，并在修复后公开致谢（如果你希望匿名请注明）。

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| v0.0.x | ✅ 安全修复 |

## 安全检查清单

- dsh-tui 是纯客户端：不持有 API Key、不存储凭据，模型调用全部经由宿主
- 连接信任基于 Host 头为 loopback —— 请勿将 `--url` 指向不受信任的地址
- 审批/提问由用户确认：所有 sandbox 升级请求都会弹出审批框，请勿盲目放行
- 会话权限保持最小化：默认 `workspace-write`，`danger-full-access` 仅按需临时使用
- 不要将宿主地址、会话 id、配置信息提交到公开仓库
